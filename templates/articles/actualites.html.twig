{% extends 'base.html.twig' %}

{% block title %}L'actualité avec Lille du Pacifique{% endblock %}

{% block body %}
    <section>
        <div class="container">
        <h1>L'actualité avec Lille du Pacifique 3</h1>
            <div>
            {% for articles in articles %}

                <div class="itemarticle col-12  col-lg-4 ">
                    {% include 'composants/cartearticle.html.twig' with {'articles': articles} %}
                </div>

            {% endfor %}
            </div>
            <div class="d-flex justify-content-center">
                {% do articles.setPageRange(2) %}
                {{ knp_pagination_render(articles, 'base/pagination.html.twig')}}
            </div>
        </div>
    </section>
{% endblock %}
