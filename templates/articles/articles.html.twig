{% extends 'base.html.twig' %}

{% block title %}L'actualité avec Lille du Pacifique{% endblock %}

{% block body %}
    <section class="listarticle">
        <div class="container">
        <h1>{{ categories.nom}}</h1>
        <p>{{ categories.description}}</p>
            <div class="row ">
            {% for article in articles %}

                <div class="itemarticle  col-md-6 col-lg-4 ">
                    {% include 'composants/cartearticle.html.twig' with {'article': article} %}
                </div>

            {% endfor %}
            </div>
            <div class="d-flex justify-content-center">
                {% do articles.setPageRange(2) %}
                {{ knp_pagination_render(articles, 'base/pagination.html.twig')}}
            </div>
        </div>
    </section>
{% endblock %}

